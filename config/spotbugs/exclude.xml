<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter>
    <!-- AES/CBC without authentication is a known trade-off in this project.
         The time-lock mechanism provides the integrity guarantee, not the cipher. -->
    <Match>
        <Class name="org.louis.EncryptDecrypt"/>
        <Bug pattern="CIPHER_INTEGRITY"/>
    </Match>

    <!-- SecureRandom is intentionally created per-call in generateKey() and generateIV().
         SpotBugs incorrectly flags SecureRandom under DMI_RANDOM_USED_ONLY_ONCE, which is
         intended for java.util.Random. A fresh SecureRandom per call is acceptable here. -->
    <Match>
        <Class name="org.louis.EncryptDecrypt"/>
        <Bug pattern="DMI_RANDOM_USED_ONLY_ONCE"/>
    </Match>

    <!-- Scanner(System.in) without an explicit charset is intentional in this console app:
         the terminal's default encoding is the correct one to use for interactive user input. -->
    <Match>
        <Class name="org.louis.Main"/>
        <Bug pattern="DM_DEFAULT_ENCODING"/>
    </Match>
</FindBugsFilter>
